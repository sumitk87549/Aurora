# Production Configuration for Render Deployment
# This file will be used when the 'prod' profile is active

# Server Configuration (Render provides PORT env var)
server.port=${PORT:8081}

# Database Configuration - Neon PostgreSQL
spring.datasource.url=${DATABASE_URL:jdbc:postgresql://ep-morning-queen-a1mzyf4v-pooler.ap-southeast-1.aws.neon.tech:5432/auroraflames?sslmode=require}
spring.datasource.username=${DB_USERNAME:postgres}
spring.datasource.password=${DB_PASSWORD:npg_FcAUEC4QL8hy}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=false

# JWT Configuration
jwt.secret=${JWT_SECRET:myVerySecureSecretKeyThatIsLongEnoughForJWT256BitSecurity12345}
jwt.expiration=86400000

# File Upload Configuration
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# CORS Configuration - Vercel Frontend
spring.web.cors.allowed-origins=${FRONTEND_URL:https://auroraflames.vercel.app}
spring.web.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
spring.web.cors.allowed-headers=*
spring.web.cors.allow-credentials=true

# Razorpay Configuration
razorpay.key.id=${RAZORPAY_KEY_ID:rzp_test_YOUR_KEY_HERE}
razorpay.key.secret=${RAZORPAY_KEY_SECRET:YOUR_SECRET_HERE}
razorpay.webhook.secret=${RAZORPAY_WEBHOOK_SECRET:YOUR_WEBHOOK_SECRET}

# Email Configuration
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME:aurora.flames01@gmail.com}
spring.mail.password=${MAIL_PASSWORD:izlt otws odps lgfd}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true

# OAuth2 Configuration
spring.security.oauth2.client.registration.google.client-id=${GOOGLE_CLIENT_ID:YOUR_GOOGLE_CLIENT_ID}
spring.security.oauth2.client.registration.google.client-secret=${GOOGLE_CLIENT_SECRET:YOUR_GOOGLE_CLIENT_SECRET}
spring.security.oauth2.client.registration.google.scope=profile, email

spring.security.oauth2.client.registration.facebook.client-id=${FACEBOOK_CLIENT_ID:YOUR_FACEBOOK_CLIENT_ID}
spring.security.oauth2.client.registration.facebook.client-secret=${FACEBOOK_CLIENT_SECRET:YOUR_FACEBOOK_CLIENT_SECRET}
spring.security.oauth2.client.registration.facebook.scope=public_profile, email

# Business Configuration
aurora.business.name=Aurora Flames
aurora.business.email=${BUSINESS_EMAIL:aurora.flames01@gmail.com}
aurora.business.phone=+91-8058073644
aurora.business.instagram=@auroraflames
aurora.business.address=Pearl Residencynear kota, Kunadi, Electricity Board Area, Kota, Rajasthan 324008

# Delivery settings
aurora.delivery.estimated-days=5
aurora.delivery.free-shipping-threshold=999

# Admin notification email
aurora.admin.email=${ADMIN_EMAIL:aurora.flames01@gmail.com}

# Thymeleaf configuration
spring.thymeleaf.prefix=classpath:/templates/
spring.thymeleaf.suffix=.html
spring.thymeleaf.mode=HTML
spring.thymeleaf.encoding=UTF-8
spring.thymeleaf.cache=true
